# Google Maps JavaScript API key, used to render the map (public/exposed to the client).
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Google Gemini API key, used by /api/compare to evaluate submitted quest photos.
GEMINI_API_KEY=your-gemini-api-key
